<?php
/**
 * Template for disabled Comfino plugin payment fields.
 *
 * Renders nothing — used as an empty placeholder when the plugin is inactive
 * or not yet configured, so the checkout payment method field area stays blank.
 */

if (!defined('ABSPATH')) {
    exit;
}
